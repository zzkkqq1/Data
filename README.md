# Data
This dataset is provided in the file `labels_with_features.csv`. It contains image-level labels and extracted structural features for rock-filled subgrade compaction assessment.

Each row corresponds to one surface image sample. The file includes the image filename, the target compaction-related label, and a series of image-derived structural descriptors describing void distribution, grayscale characteristics, gradient information, dark-region proportion, and texture complexity.
